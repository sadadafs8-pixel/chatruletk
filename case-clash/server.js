const http=require('http'),fs=require('fs'),path=require('path');const root=__dirname;http.createServer((req,res)=>{let p=req.url==='/'?'index.html':req.url.slice(1);let f=path.join(root,p);fs.readFile(f,(e,d)=>{if(e){res.statusCode=404;return res.end('404')}res.setHeader('Content-Type',p.endsWith('.html')?'text/html; charset=utf-8':'text/plain');res.end(d)})}).listen(process.env.PORT||3000,'0.0.0.0');